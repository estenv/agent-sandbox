[package]
name = "sandbox-tests"
version = "0.1.0"
edition = "2021"
description = "End-to-end sealed sandbox integration tests using srt"
license = "MIT"
publish = false

[dependencies]
serde_json = "1"
